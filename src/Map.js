function Ee() {
  const numbers = [2, 1, 0, 8, 7];
  //map
  const item = numbers.map((n) => ({ numberss: n }));
  console.log(item);
  const i = numbers.filter((n) => n > 4).map((n) => ({ numberss: n }));
  console.log(i);
}
export default Ee;
